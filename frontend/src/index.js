// AI Doctor is served as a self-contained static site from /public
// (index.html + styles.css + the app's own JS modules + Firebase).
// The React bundle is intentionally a no-op so it does not inject any
// global styles or mount over the static markup.
export {};
